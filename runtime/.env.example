# Your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Point at the local proxy to intercept all API traffic (Figure 1 — The Setup)
# Start the proxy first:  bun run proxy
# Then uncomment:
# ANTHROPIC_BASE_URL=http://localhost:8765

# Proxy port (default: 8765)
# PROXY_PORT=8765
